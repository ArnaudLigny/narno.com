{% extends 'page.html.twig' %}

        {%- block content ~%}
        {%~ include 'partials/lastpost.html.twig' ~%}
        <hr/>
        {%~ include 'partials/lastvideos.html.twig' ~%}
        {%~ include 'partials/lastposts.html.twig' ~%}
        {#
        {%~ include 'partials/search.html.twig' -%}
        #}
        {%- endblock content ~%}
