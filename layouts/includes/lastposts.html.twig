<div class="lastposts">
  <h2>Derniers billets du blog</h2>
  <ul>
  {% for post in site.pages|slice(0, 10)|filterBy('section', 'blog')|sortByDate %}
    <li><a href="{{ url(post) }}">{{ post.title }}</a></li>
  {% endfor %}
  </ul>
</div>
