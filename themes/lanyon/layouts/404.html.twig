{% extends 'page.html.twig' %}

        {%- block content ~%}
        <div class="page">
          <h1 class="page-title"></h1>{% trans "404: Page not found" %}
          <p class="lead">{% trans "Sorry, we've misplaced that URL or it's pointing to something that doesn't exist." %} <a href="{{ site.baseurl }}">{% trans "Head back home" %}</a> {% trans "to try finding it again." %}</p>
        </div>
        {%- endblock content ~%}
